<div class="w-64 bg-discord-dark flex-shrink-0 flex flex-col border-r border-gray-800">
    <div class="p-6">
        <div class="flex items-center gap-3 mb-6">
            <% if (guild.icon) { %>
                <img src="https://cdn.discordapp.com/icons/<%= guild.id %>/<%= guild.icon %>.png"
                    class="w-10 h-10 rounded-full">
                <% } else { %>
                    <div
                        class="w-10 h-10 rounded-full bg-gray-600 flex items-center justify-center text-white font-bold">
                        <%= guild.name.charAt(0) %>
                    </div>
                    <% } %>
                        <div class="overflow-hidden">
                            <h2 class="font-bold text-white truncate">
                                <%= guild.name %>
                            </h2>
                        </div>
        </div>

        <nav class="space-y-1">
            <a href="/dashboard/<%= guild.id %>"
                class="flex items-center gap-3 px-4 py-3 rounded text-gray-400 hover:bg-discord-darker hover:text-white transition <%= active === 'overview' ? 'bg-discord-darker text-white' : '' %>">
                <span>⚙️</span> Overview
            </a>

            <div class="pt-4 pb-2 px-4 text-xs font-bold text-gray-500 uppercase">Plugins</div>

            <a href="/dashboard/<%= guild.id %>/welcome"
                class="flex items-center gap-3 px-4 py-3 rounded text-gray-400 hover:bg-discord-darker hover:text-white transition <%= active === 'welcome' ? 'bg-discord-darker text-white' : '' %>">
                <span>👋</span> Welcome & Goodbye
            </a>

            <a href="/dashboard/<%= guild.id %>/tickets"
                class="flex items-center gap-3 px-4 py-3 rounded text-gray-400 hover:bg-discord-darker hover:text-white transition <%= active === 'tickets' ? 'bg-discord-darker text-white' : '' %>">
                <span>🎫</span> Ticket System
            </a>

            <a href="/dashboard/<%= guild.id %>/security"
                class="flex items-center gap-3 px-4 py-3 rounded text-gray-400 hover:bg-discord-darker hover:text-white transition <%= active === 'security' ? 'bg-discord-darker text-white' : '' %>">
                <span>🛡️</span> Security
            </a>

            <a href="/dashboard/<%= guild.id %>/levels"
                class="flex items-center gap-3 px-4 py-3 rounded text-gray-400 hover:bg-discord-darker hover:text-white transition <%= active === 'levels' ? 'bg-discord-darker text-white' : '' %>">
                <span>📊</span> Levels (MEE6)
            </a>
        </nav>
    </div>

    <div class="mt-auto p-4 border-t border-gray-800">
        <a href="/dashboard" class="flex items-center gap-2 text-gray-400 hover:text-white transition text-sm">
            <span>&larr;</span> Back to Servers
        </a>
    </div>
</div>